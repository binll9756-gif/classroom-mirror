# 怎么使用课镜

> 给师范生用：对着 3 个 AI 学生试讲 → 系统量化你的教学行为 → 出带证据的诊断报告。
> 全过程**数据不出本机**（用的本地模型）。

---

## 🖱 最简单的用法：双击

在项目文件夹 `C:\AI\课镜项目\01_仓库代码与文档\` 里，有两个文件：

| 双击这个 | 作用 |
|---|---|
| **`① 启动网页版.bat`** | 启动后在浏览器打开 http://127.0.0.1:8000 |
| **`② 启动命令行版.bat`** | 在窗口里直接对着 AI 学生试讲 |

**第一次用之前，还是要先做一次「一、前置条件」里的 `pip install`。**

> ⚠️ **不要**在 `C:\Users\你的用户名` 这种地方敲 `python run_web.py` ——
> 那个目录下没有这个文件，会报 `can't open file`。
> 要么先 `cd` 到项目目录，要么直接用上面的 .bat 双击。

---

## 一、前置条件（只做一次）

### 1. 装依赖

```powershell
cd C:\AI\课镜项目\01_仓库代码与文档
pip install -r requirements.txt
```

### 2. 确认 Ollama 在跑、模型在

```powershell
ollama list          # 应该能看到 qwen3.5:9b
```

看不到模型就先拉一个：
```powershell
ollama pull qwen3.5:9b
```

### 3. 确认配置

目录下应该有 `.env`（没有就把 `.env.example` 复制一份改名）。内容：

```
KJ_PROVIDER=ollama
OPENAI_BASE_URL=http://127.0.0.1:11434
KJ_MODEL=qwen3.5:9b
OPENAI_API_KEY=ollama
```

> ⚠️ **一定要写 `127.0.0.1`，不要写 `localhost`**。
> Windows 会把 `localhost` 先解析成 IPv6 再回退 IPv4，**每次请求白等 2 秒**（实测 4.13s → 1.77s）。

### 4. 自检

```powershell
python -c "import sys;sys.path.insert(0,'.');from kj import llm;print(llm.status())"
```

应该输出类似：`本地 Ollama · qwen3.5:9b · 在线（数据不出本机）`

---

## 二、网页版（推荐）

**双击 `① 启动网页版.bat`**，或者：

```powershell
cd C:\AI\课镜项目\01_仓库代码与文档
python run_web.py
```

然后浏览器打开 **http://127.0.0.1:8000**

### 操作步骤（照着做一遍就懂了）

| 步 | 你做什么 | 会看到什么 |
|---|---|---|
| 1 | 点右上角 **「① 生成学生」** | 左侧出现 3 个学生：小林（迷思型）、小周（沉默型）、阿豪（过度积极型），每张卡上写着**预设错误** |
| 2 | 在底部输入框**讲一句话**，回车 | 你的话进入对话流 |
| 3 | **提一个问题**，比如「(-3)×(-2) 等于多少？小林你来说说。」 | 页面弹出 **⏱ 等待计时器**，有按钮「让学生回答」 |
| 4 | **先别急着点** —— 等 3 秒再点「让学生回答」 | 学生答错：**"负号乘负号不就消失了吗？"** 右侧实时滚动 FIAS 标签 |
| 5 | 继续讲、继续问，试试**只叫小林** | 右侧会提醒「**阿豪 还没被叫到**」 |
| 6 | 点右上角 **「② 结束试讲并出报告」** | 等 4~5 秒，下方出现**评价报告** |

### 报告里看什么

- **★ IDR 间接/直接影响比** —— 核心指标。旁边有对照刻度：专家人类教师 1.473、新手 0.885
- **被忽略的学生** —— 有没有人整节课没被叫到
- **每条诊断结论** —— 都带 `[证据 turn N]`，能对回原话
- **评分** —— 由代码计算，同一份记录算两次结果完全一样

### 有两条行为值得刻意练

| 行为 | 系统的反应 |
|---|---|
| 提问后**立刻**自己接话 | 记为**自问自答**（真抢答），学生会失去回答机会 |
| 提问后**等 3 秒**再让学生答 | 平均等待时间上升，报告里会变成正面评价 |

---

## 三、命令行版（不用开浏览器）

```powershell
python run_teach.py
```

也可以直接解析自己的教案：
```powershell
python run_teach.py D:\我的教案.pdf
```

### 可用指令

| 输入 | 作用 |
|---|---|
| 直接打字 | 老师说话 |
| `/叫 小林` | 下一句指定点名小林 |
| `/走神` | 让某学生走神（记为行为事件） |
| `/状态` | 看三个学生此刻的认知状态 |
| `/报告` | 结束本轮，出指标与报告 |
| `/帮助` | 指令说明 |
| `/退出` | 结束 |

**提问后的操作**：会提示「让谁答？」
- **直接回车** → 交给学生答（停顿时长记为等待时间）
- **输入内容** → 你自己接着说（记为自问自答）

---

## 四、脚本化演示（不用输入，自动跑完）

```powershell
python run_demo.py
```

自动跑两轮试讲并输出对比表。适合快速验证环境、或者录屏时当素材。

---

## 五、想换成云端模型

改 `.env`：

```
KJ_PROVIDER=openai
OPENAI_BASE_URL=https://api.deepseek.com/v1
KJ_MODEL=deepseek-chat
OPENAI_API_KEY=sk-你的key
```

代码不用动。**注意**：换成云端后数据会离开本机，隐私优势就没了。

---

## 六、给别人用（部署）

```powershell
python run_web.py --host 0.0.0.0 --port 8000
```

然后别人用 `http://你的局域网IP:8000` 访问。
要让外网访问需要内网穿透或放到服务器上（比赛要求的「可公开访问的测试账号」就是这个）。

---

## 七、常见问题

| 现象 | 原因 / 怎么办 |
|---|---|
| `llm.status()` 显示"连不上" | Ollama 没启动。运行 `ollama serve` 或打开 Ollama 应用 |
| 提示模型不可用，学生说的是模板话 | 同上；模板模式下 **FIAS 指标仍真实计算**，不影响指标练习 |
| 回答很慢（>5 秒/句） | ① 检查 `.env` 是不是写了 `localhost`（要改 `127.0.0.1`）② `ollama ps` 看模型是不是被卸载了 |
| 报告要等 4~5 秒 | 正常。报告里每条诊断都要模型措辞，已做批量优化（原来 10~30 秒） |
| 中文乱码 | PowerShell 先执行 `$env:PYTHONIOENCODING='utf-8'` |
| 端口被占用 | `python run_web.py --port 8001` |

---

## 八、系统里你可以随时自查的两个"科学边界"

1. **只有 IDR 能代表你的教学能力** —— 它在你自己话语内部分子分母，与学生说了多少话无关。
   TT / ST 受"AI 学生被设定得多爱说话"影响，**SIR 更是由系统参数决定**，它们只用于描述课堂情境。
2. **指标由代码计算，不由模型算** —— 所以同一份记录重复计算，结果永远一致，可以复核、可以申诉。
