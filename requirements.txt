# Kejing (AI micro-teaching coach) dependencies
# Install: pip install -r requirements.txt
#
# NOTE: keep this file ASCII-only. pip reads requirements.txt with the
# system locale encoding on Windows (GBK here), so non-ASCII comments
# break the install with UnicodeDecodeError.

# ---- core (required) ----
PyYAML>=6.0

# ---- model access ----
# Local Ollama uses its native HTTP API and needs no extra package.
# This one is only needed for OpenAI-compatible cloud endpoints.
openai>=1.0

# ---- web UI (needed by run_web.py) ----
fastapi>=0.110
uvicorn>=0.27

# ---- lesson parsing / document export ----
pypdf>=4.0                  # read PDF lesson plans
python-docx>=1.1            # read Word lesson plans; also md -> docx export
# Do NOT use PyMuPDF: it is AGPL-licensed and would affect service distribution.

# ---- optional: auto screenshots (tools/screenshot.py) ----
# pip install playwright
# playwright reuses the local Edge/Chrome, no browser download needed.
