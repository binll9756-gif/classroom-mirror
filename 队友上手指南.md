# 队友上手指南

> 你是新加入的队友，这份文件告诉你：**怎么把代码拿到手、怎么改、怎么传回来、你能干什么。**
> 全程只需要 6 条 git 命令。不用怕 git。

---

## 一、先把代码拿到手（二选一）

### 方式 A：用 git（推荐，以后能同步更新）

先装 Git：https://git-scm.com/download/win （一路下一步即可）

```bash
git clone <仓库地址>
cd classroom-mirror      # 进到项目目录
```

### 方式 B：不会 git 就先下载 zip

在仓库页面点绿色按钮 **Code → Download ZIP**，解压即可。
缺点：**别人改了代码你拿不到最新版**，只能重新下载。所以建议还是学方式 A（真的只要 6 条命令）。

### 拿到代码后第一件事：跑起来

```powershell
pip install -r requirements.txt
$env:PYTHONIOENCODING='utf-8'
python tests/test_fias.py     # 应该看到全部 ✅
python run_demo.py            # 应该看到两轮对比
```

**跑不通就是第一个要解决的问题**——把完整报错发群里。

---

## 二、git 是什么（大白话）

想象一个**共享网盘 + 修改历史 + 冲突提醒**的组合：

| 概念 | 大白话 |
|---|---|
| **仓库（repository）** | 这个项目的云端文件夹 |
| **clone** | 把云端文件夹整个下载到你的电脑（只需一次） |
| **pull** | 把别人后来的改动同步到你的电脑 |
| **commit** | 给你这次的修改拍一张"快照"并写一句说明 |
| **push** | 把你的快照传到云端，别人就能看到 |
| **分支（branch）** | 你的私人工作台，不影响别人 |

**核心习惯**：**每天开工前先 `pull`，做完一件事就 `commit` + `push`。**

---

## 三、你真正要用的 6 条命令

```bash
# 1) 开工前：把别人的改动同步下来（每天第一件事）
git pull

# 2) 看看我改了哪些文件
git status

# 3) 把改动加入待提交清单（. 表示所有改动）
git add .

# 4) 拍快照 + 写说明（说明要写清楚改了什么）
git commit -m "feat: 把 FIAS 分类换成 LLM 调用"

# 5) 传到云端
git push

# 6) 看看历史记录
git log --oneline -10
```

### 万一提示要登录

第一次 `push` 会让你登录 GitHub。推荐装图形化工具 **[GitHub Desktop](https://desktop.github.com/)**，或者按提示用浏览器授权。**不要把密码或 token 发到群里。**

### 万一出错了

```bash
# 我想放弃本地所有改动，恢复成云端的样子（慎用，会丢改动！）
git checkout -- .

# 我 commit 了但还没 push，想撤销这次 commit（保留改动）
git reset --soft HEAD~1
```

**卡住了就把 `git status` 的完整输出发群里** —— 这句话能诊断 90% 的问题。

---

## 四、commit 说明怎么写（简单约定）

格式：`类型: 一句话说明`

| 类型 | 什么时候用 |
|---|---|
| `feat:` | 加了新功能（例：`feat: 学生台词接入真模型`） |
| `fix:` | 修了 bug（例：`fix: 走神频率过高刷屏`） |
| `docs:` | 只改了文档（例：`docs: 补充队友上手指南`） |
| `test:` | 加或改了测试（例：`test: 补 FIAS 边界用例`） |
| `refactor:` | 重构，功能没变 |

**写得越具体，别人越容易知道你在干什么。**

---

## 五、⚠️ 千万别提交这些东西

| 不要提交 | 为什么 |
|---|---|
| **任何 API Key / token / 密码** | 上传后立刻泄露。本项目代码从**环境变量**读 Key，不要改死 |
| `kejing.db`、`__pycache__/` | 运行时产物，每次跑都会重新生成（`.gitignore` 已挡掉） |
| 几百 MB 的视频 | GitHub 单文件上限 100MB。**视频用网盘/学习通传**，仓库里只放脚本 |
| **真实姓名、学号、指导教师** | 比赛要求双盲；而且公开仓库谁都能看 |
| 从网上下的教材 PDF | 有版权风险 |

> 如果不小心提交了 Key：**立刻去模型平台后台吊销那个 Key**，然后再删文件——因为 git 历史里还留着。

---

## 六、你可以认领的任务（按"零依赖、能立刻开工"排序）

| 优先级 | 任务 | 交付什么 | 依赖谁 |
|---|---|---|---|
| 🥇 | **FIAS 金标准语料**：准备 60 条课堂话语 + 人工标注九类 + 写标注理由 | 一个表格，用来验证"编码准不准" | **不需要任何人** |
| 🥈 | **「负负得正」教学素材**：教案定稿 + 每个学生 3~5 句典型台词 + 3 个学生画像 | 前端可直接拿去填假数据 | **不需要任何人** |
| 🥉 | **试讲教室前端原型**（先用假数据） | 一个能点、能截图的页面 | **不需要任何人** |
| 4 | **迷思概念库送审**：请数学教育方向老师审 `knowledge/misconceptions.yaml` | 老师批注版 | 需要你去找老师 |
| 5 | **接入真模型**：让 `students.py` 的台词走 LLM（状态仍由代码控制） | 小林答错后坚持不改 | 需要 API Key |
| 6 | **FIAS 分类换 LLM**：把 `fias.rule_classify` 换成一次模型调用 + 人工一致性比对 | 可报出的一致率 | 需要任务 1 的语料 |
| 7 | **用户测试**：组织 ≥10 名师范生跑两轮 | 前后指标数据 | 需要系统可用 |
| 8 | **说明书 / 视频** | 材料定稿 | 需要用户测试数据 |

> **为什么把"语料"排第一**：它是唯一能证明"我们的编码是准的"的东西。
> 没有它，"FIAS 自动编码"这个最硬的创新点就永远无法验证。**而且它完全不需要写代码。**

---

## 七、三个约定

1. **不要直接改 `main` 分支上正在被别人改的文件。** 简单做法：动手前在群里说一句"我要改 `kj/students.py`"。
2. **每完成一件事就 push**，不要攒一周。
3. **改了影响别人的东西（比如改了指标公式），要在 commit 说明里写出来，并在群里说一声。**

---

## 八、出问题找谁 / 怎么问

问之前先做三件事：
1. `git status` 看一眼；
2. 把**完整报错**复制下来（别只截一句话）；
3. 说清"我原本想做什么、执行了什么命令、结果是什么"。

这样别人一眼就能帮你定位。
