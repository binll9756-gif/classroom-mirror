# 复制这个文件为 .env 然后按需修改（.env 已被 .gitignore 挡住，不会上传）

# ============ 方案 A：本地 Ollama（推荐）============
# 优点：免费、无限次调试、数据不出本机（合规与伦理加分）、断网也能演示
# 前提：本机装好 Ollama 并已 pull 模型（ollama list 能看到）
# ⚠️ 一定要用 127.0.0.1，不要用 localhost：
#    Windows 会把 localhost 先解析成 IPv6 ::1，连不上再回退 IPv4，
#    每次请求白等约 2 秒（实测学生台词 4.13s → 1.77s）
KJ_PROVIDER=ollama
OPENAI_BASE_URL=http://127.0.0.1:11434
KJ_MODEL=qwen3.5:9b
OPENAI_API_KEY=ollama

# ============ 方案 B：云端 OpenAI 兼容接口 ============
# 换成下面这样即可（DeepSeek / 通义 / 智谱 都行）：
# KJ_PROVIDER=openai
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# KJ_MODEL=deepseek-chat
# OPENAI_API_KEY=sk-你的key

# 可选：单次请求超时（秒），本地模型首次加载慢，默认 180
# KJ_TIMEOUT=180
