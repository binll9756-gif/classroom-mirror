# 分享给队友

> 仓库：**https://github.com/binll9756-gif/classroom-mirror**
> 可见性：**Public（公开）** —— **队友不需要邀请，直接克隆即可**。

---

## 一、把这段话直接发给队友

> 复制下面整段发给队友（把 `<队友>` 换成他的称呼）：

```
课镜项目仓库（公开，直接下载）：
https://github.com/binll9756-gif/classroom-mirror

第一步：装 Git（https://git-scm.com/download/win，一路下一步即可）
第二步：打开 PowerShell，依次执行：

  git clone https://github.com/binll9756-gif/classroom-mirror.git
  cd classroom-mirror
  pip install -r requirements.txt
  $env:PYTHONIOENCODING='utf-8'
  python tests/test_fias.py
  python run_demo.py

跑通后，按这个顺序读三份文件：
  1) README.md          —— 项目是什么、核心设计、当前进度
  2) 队友上手指南.md     —— git 6 条命令 + 你能认领的任务
  3) 00_先看这里.md      —— 整个文件夹的导航

不会 git 也可以先点页面上的 Code → Download ZIP 下载解压。

要往仓库传代码时需要 GitHub 账号：
  - 最简单：装 GitHub Desktop（https://desktop.github.com/）
  - 或者把用户名告诉我，我加你为协作者

不要提交：API Key、kejing.db、__pycache__、大视频、真实姓名。
卡住了把 git status 的完整输出发群里。
```

---

## 二、关于仓库可见性（重要变化）

**本仓库现在是 Public（公开）。** 这意味着：

| | 说明 |
|---|---|
| ✅ **队友不用邀请** | 任何人拿到链接都能克隆，省掉邀请步骤 |
| ✅ 已做去身份化 | 全仓库扫描 0 处敏感词命中，且**重建过 git 历史**（旧记录里也翻不出学校名） |
| ⚠️ **任何人都能看** | 包括评委、其他参赛队。所以**不要往仓库里放身份信息、密钥、内部材料** |
| ⚠️ **赛事资料不在仓库里** | 官方实施方案、评分表、报名与提交指引等含学校与赛事信息，**只保存在团队本地**，需要时向队长索取 |

### 如果要改成私有

```powershell
gh repo edit binll9756-gif/classroom-mirror --visibility private --accept-visibility-change-consequences
```

改私有后，队友需要被邀请才能访问：
```powershell
gh api repos/binll9756-gif/classroom-mirror/collaborators/<队友用户名> -X PUT -f permission=push
```

---

## 三、队友拿到之后，建议这样分工

| 人 | 任务 | 依赖 |
|---|---|---|
| 队友 1 | **FIAS 金标准语料**：60 条课堂话语 + 人工标注九类 + 写标注理由 | 零依赖，可立刻开工 |
| 队友 2 | **「负负得正」教学素材**：教案定稿 + 每个学生 3~5 句台词 + 3 个学生画像 | 零依赖，可立刻开工 |
| 队长 | 说明书统稿 + 用户测试组织 + 与老师对接 | — |

> 详细任务清单（8 项，按优先级）见 `队友上手指南.md` 第六节。

---

## 四、协作约定（三条，够用了）

1. **动手前在群里说一句**"我要改 `kj/students.py`"，避免两人改同一个文件。
2. **每完成一件事就 `commit` + `push`**，不要攒一周。
3. **改了影响别人的东西（比如指标公式）**，要在 commit 说明里写清，并在群里说一声。

> 更细的 git 操作见 `队友上手指南.md` 第三节（只有 6 条命令）。

---

## 五、安全提醒

| 事项 | 说明 |
|---|---|
| **不要提交 API Key** | `kj/llm.py` 从环境变量读 Key。若误提交了，**第一件事是去模型平台后台吊销 Key**（删文件没用，git 历史还留着）。 |
| **视频不进仓库** | GitHub 单文件上限 100MB。视频用网盘/官方平台传，仓库里只放脚本。 |
| **提交给评委的材料要双盲** | 材料本身和**现场答辩口播**都不能出现姓名、学院、年级、指导教师。 |
| **不要放内部材料** | 赛事规则、官方文件、团队内部讨论记录，都留在本地。 |

---

## 六、以后怎么同步

```powershell
# 每天开工前
git pull

# 做完一件事
git add .
git commit -m "feat: 一句话说清改了什么"
git push
```

**长期不 push 的代价**：别人改了同一份文件，你就会遇到冲突——那时候解决起来比每天 push 麻烦十倍。
